export function markBlocked(_issueNumber: number, _question: string): Promise<void> {
  // TODO Phase 2:
  // 1. Add cc:blocked label to issue
  // 2. Post structured question as a comment
  // 3. Update meta.json with blocked: true + blocked_question
  throw new Error('Not yet implemented');
}
