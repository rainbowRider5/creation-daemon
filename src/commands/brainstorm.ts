export function brainstormCommand(_topic?: string): Promise<void> {
  // TODO Phase 1:
  // 1. Start interactive Claude session with brainstorm prompt
  // 2. Collaboratively shape the idea
  // 3. Generate vision doc in docs/visions/
  // 4. Create draft tickets on GitHub
  console.log('cc brainstorm — not yet implemented');
  return Promise.resolve();
}
