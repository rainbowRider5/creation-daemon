export function statusCommand(): Promise<void> {
  // TODO Phase 0:
  // 1. Fetch all open cd:* issues from GitHub
  // 2. Group by status label
  // 3. Print board to terminal (columns: draft, refined, ready, in-progress, in-review, blocked)
  console.log('/cd-status — not yet implemented');
  return Promise.resolve();
}
