import { describe, it } from 'vitest';

describe('blocker', () => {
  it.todo('adds cd:blocked label to issue');
  it.todo('posts structured question as comment');
  it.todo('updates meta.json with blocked state');
});
