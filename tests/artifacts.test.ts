import { describe, it } from 'vitest';

describe('artifacts', () => {
  it.todo('creates issue directory if it does not exist');
  it.todo('assigns sequential numbering to artifacts');
  it.todo('updates meta.json when writing an artifact');
  it.todo('reads all artifacts for an issue');
});
